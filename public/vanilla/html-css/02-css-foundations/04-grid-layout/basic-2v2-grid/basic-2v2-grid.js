import { setupExercise } from "../../../../scripts/html-css-exercise-handler.js";

setupExercise({
  fileKey: "02-layout-and-css-models",
  exerciseName: "Basic 2v2 grid",
  statement:
    "Create a container that displays 4 blocks in a 2x2 grid. All blocks must be the same size.",
});

// ! Рішення ----------
