import { setupExercise } from "../../../../scripts/vanilla-exercise-handler.js";

setupExercise({
  fileKey: "11-local-storage",
  exerciseName: "Getting an array from localStorage",
  statement:
    "Store an array of numbers [1, 2, 3, 4, 5] in localStorage. Retrieve it and print the sum of the elements.",
});

// ! Рішення ----------
